services:
  - type: web
    name: nadim-downloader
    runtime: python
    buildCommand: pip install -r requirements.txt && yt-dlp --update-to nightly
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: NODE_VERSION
        value: 18.0.0
